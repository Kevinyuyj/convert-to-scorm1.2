interface:
  display_name: "SCORM 2004 to 1.2"
  short_description: "Repair SCORM assets, rebuild packages, and debug SCORM runtime behavior"
  default_prompt: "Use $scorm2004-to-12 to inspect this SCORM ZIP, repair image loading, rebuild a safe package, and check LMS smoke-test gates for launch, resume, completion, score, and learning time."
policy:
  allow_implicit_invocation: true
