interface:
  display_name: "SCORM 2004 to 1.2"
  short_description: "Repair SCORM assets and SCORM 1.2 runtime tracking"
  default_prompt: "Use $scorm2004-to-12 to inspect this SCORM ZIP, repair asset paths or SCORM 1.2 runtime tracking, verify bundled runtime behavior, and rebuild a safe package."
policy:
  allow_implicit_invocation: true
